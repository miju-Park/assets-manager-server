# Migration `20200920103940-checking-account`

This migration has been generated by mjpark at 9/20/2020, 7:39:40 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Assets" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "type" TEXT NOT NULL,
    "bank" TEXT NOT NULL,
    "title" TEXT NOT NULL,
    "startdate" DATETIME NOT NULL,
    "duedate" DATETIME NOT NULL,
    "initialDeposit" INTEGER NOT NULL DEFAULT 0,
    "payment" INTEGER NOT NULL DEFAULT 0,
    "balance" REAL NOT NULL DEFAULT 0,
    "currency" INTEGER NOT NULL,
    "ticker" TEXT NOT NULL DEFAULT '',
    "count" INTEGER NOT NULL DEFAULT 0,
    "currentPrice" REAL NOT NULL DEFAULT 0,
    "averagePrice" REAL NOT NULL DEFAULT 0,
    "ownerId" INTEGER,

    FOREIGN KEY ("ownerId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
);
INSERT INTO "new_Assets" ("id", "type", "bank", "startdate", "duedate", "balance", "currency", "ownerId", "title", "initialDeposit", "payment", "ticker", "count", "currentPrice", "averagePrice") SELECT "id", "type", "bank", "startdate", "duedate", "balance", "currency", "ownerId", "title", "initialDeposit", "payment", "ticker", "count", "currentPrice", "averagePrice" FROM "Assets";
DROP TABLE "Assets";
ALTER TABLE "new_Assets" RENAME TO "Assets";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200920102621-checking-account..20200920103940-checking-account
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db{
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
@@ -22,13 +22,13 @@
 	startdate DateTime
 	duedate DateTime
   initialDeposit Int @default(0)
   payment Int @default(0)
-	balance Int @default(0)
+	balance Float @default(0)
 	currency Int
   ticker String @default("")
   count Int @default(0)
-  currentPrice Int @default(0)
-  averagePrice Int @default(0)
+  currentPrice Float @default(0)
+  averagePrice Float @default(0)
   owner User? @relation(fields:[ownerId], references:[id])
   ownerId  Int?
 }
```


