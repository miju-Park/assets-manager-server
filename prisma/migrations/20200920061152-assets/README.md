# Migration `20200920061152-assets`

This migration has been generated by mjpark at 9/20/2020, 3:11:52 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Assets" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "ownerId" INTEGER,

    FOREIGN KEY ("ownerId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "password" TEXT NOT NULL
)

CREATE TABLE "CheckingAccount" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "bank" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "startdate" DATETIME NOT NULL,
    "duedate" DATETIME NOT NULL,
    "balance" INTEGER NOT NULL,
    "currency" INTEGER NOT NULL,
    "assetsId" INTEGER,

    FOREIGN KEY ("assetsId") REFERENCES "Assets"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "Assets_ownerId_unique" ON "Assets"("ownerId")

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200920061152-assets
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,31 @@
+datasource db{
+  provider = "sqlite"
+  url = "***"
+}
+generator client {
+  provider = "prisma-client-js"
+}
+model Assets {
+  id  Int @id @default(autoincrement())
+  checkingAccount CheckingAccount[]
+  owner  User?  @relation(fields:[ownerId], references:[id])
+  ownerId  Int?
+}
+
+model User {
+  id  Int @id @default(autoincrement())
+  name  String
+  email String  @unique
+  password  String
+  assets Assets
+}
+
+model CheckingAccount {
+	id Int @id
+	bank String
+	name String
+	startdate DateTime
+	duedate DateTime
+	balance Int
+	currency Int
+}
```


